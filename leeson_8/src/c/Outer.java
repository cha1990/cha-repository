package c;

public class Outer {

	// not just like any other class
	// each instance of inner belongs to an instance of outer
	public class Inner {

	}

	// just like any other class (because of static)
	public static class Nested {

	}

}
